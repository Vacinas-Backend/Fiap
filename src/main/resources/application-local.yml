info:
  app:
    name: Vacinas
    description: Vacinas

server:
  port: 8090

scheduled:
  cron: 0 0 * * * *

spring:
  datasource:
    driver: org.postgresql.postgresql
    url: jdbc:postgresql://localhost:5432/FiapVacinas?stringtype=unspecified
    username: postgres
    password: 123